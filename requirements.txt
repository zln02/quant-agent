# OpenClaw Trading System — Python Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.141.1
uvicorn==0.52.3

# Trading APIs
pyupbit==0.2.34
yfinance==0.2.58
alpaca-trade-api==3.2.0  # US 주식 실전/페이퍼 주문 (optional)

# Technical indicators
ta==0.11.0
pandas==2.3.3

# AI / ML
openai==2.54.0
anthropic==0.122.0  # Claude API (claude-haiku-4-5, claude-sonnet-4-6)
xgboost==3.4.1
lightgbm==4.7.0
catboost==1.2.10
web3==7.16.0
gql==4.0.0
scikit-learn==1.9.0

# Database
supabase==2.6.0

# HTTP
requests==2.34.2  # CVE-2026-25645

# Security-pinned transitives (2026-04-19 pip-audit 대응)
pyjwt==2.13.0  # CVE-2026-32597
cryptography==50.0.0  # CVE-2026-34073, 39892
authlib==1.7.2  # GHSA-jj8c-mmj3-mmgv
nltk==3.10.3  # CVE-2026-33230
pillow==12.3.0  # CVE-2026-40192
# urllib3 2.x는 alpaca-trade-api 3.0 의존성 충돌(<2 요구) — 1.x 유지.
# CVE-2025-50181/66418/66471/2026-21441은 알려진 거래소 API(Upbit/Kiwoom/Alpaca/yfinance)만
# 호출하므로 untrusted streaming 리스크 낮음. alpaca-trade-api → alpaca-py 마이그레이션 후 업 예정.
urllib3==1.26.20
pygments==2.20.0  # CVE-2026-4539
pyasn1==0.6.4  # CVE-2026-30922
curl-cffi==0.16.0  # CVE-2026-33752

# Realtime and alternative data (Phase 9)
# websockets: alpaca-trade-api requires <11, pyupbit also depends on it → transitive dep로 해결
pytrends==4.9.2

# System monitoring
psutil==6.1.1

# Utilities
python-dotenv==1.2.2
holidays==0.102  # KR 공휴일 (PR #24 silence_monitor)

# Google Sheets (optional, for sheets_logger fallback when gog CLI unavailable)
# gspread>=5.12.0
# google-auth>=2.25.0

# Excel 파일 읽기/쓰기 (optional)
openpyxl==3.1.5

# ML explainability
shap==0.52.0
numpy==2.5.2

# Metrics / monitoring
prometheus-fastapi-instrumentator==8.1.0

# 개발/테스트 의존성
pytest==9.1.1
pytest-cov==7.1.0
