# 운영 회고 — quant-agent

**운영 기간:** 2026-02-21 ~ 2026-06-13 (사람 작성 커밋 기준 실측, 129개 커밋)
**종료 사유:** 계획 종료 — 포트폴리오 정리에 따른 운영 중단
**현재 상태:** Archived (읽기 전용). 가동 중인 서비스·실시간 지표 없음.

---

## 운영 중 겪은 문제와 대응

| # | 문제 | 대응 | 결과 | 근거 |
|---|------|------|------|------|
| 1 | **LLM API 크레딧 소진** — BTC 에이전트가 `analyze_with_ai()` 응답을 매매 신호로 사용해, 크레딧 소진 시 `confidence=0`으로 매 사이클 SKIP → 자동매매 정지 | LLM 매매 신호 의존을 제거하고 `rule_based_btc_signal()` 결정론 스코어링으로 전환. KR도 회색지대 LLM 분기 삭제 | 매매 판단이 외부 API 가용성과 분리됨. 전환 시점 `pytest 88 passed` (기존 76 + 신규 12), v6.3 완료 시점 `113 passed` | 로그: `memory/2026-04-06-llm-request-rejected-your-cred.md` (2026-04-02 16:00:03 KST 크레딧 거부 원문)<br>커밋: `0b7eebf` (2026-04-08)<br>문서: `CHANGELOG.md` v6.3 Rationale |
| 2 | **환경변수 로더 산재** — `.env`가 호스트/도커/크론마다 다른 경로에서 로드되어 설정 불일치. PROD 키 누락 시 MOCK 키로 자동 대체되는 잠재 버그 포함 | 로더 탐색 경로를 통합하고 키움 설정을 SSOT로 분리(`common/kiwoom_env.py`), or-fallback 제거 | 크론·도커·호스트 실행 간 설정 일치. `tests/test_kiwoom_env.py` 6 케이스 추가 | 커밋: `a1db4ab` (2026-04-08)<br>`b7351e3` (2026-05-12)<br>`9fb69a3` (2026-05-14) |

> 저장소 밖 운영 경험(호스트 이전 등)은 기록이 없어 미기재.

### 1번 항목에서 삭제한 수치

초안에는 "8시간 장애", "폴백 13/13" 이라는 수치가 있었으나, 저장소의 로그·커밋·문서
어디에서도 **장애 지속시간과 폴백 건수를 확인할 수 없어 삭제했다.** 확인 가능한 것은
크레딧 거부 로그의 발생 시각(2026-04-02 16:00:03 KST)과 8일 뒤 전환 커밋(`0b7eebf`,
2026-04-08)까지이며, 그 사이의 정확한 중단 시간은 기록이 남아 있지 않다.

---

## 종료 시점에 남아 있던 상태

운영 종료(2026-06-13) 이후에도 `.github/workflows/update-readme.yml` 이 매일 실행되며
README 상단에 "Live Trading Performance" 표를 갱신하고 있었다. 다만 Supabase 접속
정보가 설정되지 않은 상태여서, 실제로는 **자리표시자 0값과 갱신 시각만** 기록되고
있었다 (워크플로 로그: `SUPABASE_URL/KEY not set — using placeholder stats`).

- 표의 수치가 마지막으로 실제 변경된 시점: **2026-04-06**
- 이후 갱신된 항목: 타임스탬프 한 줄뿐
- 총 자동 커밋: 130회 (2026-04-07 ~ 2026-08-14)

저장소가 계속 운영 중인 것처럼 보이는 원인이었으므로, 아카이브 시점에
정기 실행(cron)을 해제하고 해당 표를 README에서 제거했다.
워크플로 파일 자체는 실행 이력 보존을 위해 남겨 두었다(수동 실행 전용).

---

## 재현 방법

```bash
git clone https://github.com/zln02/quant-agent.git && cd quant-agent
cp .env.example .env    # 값은 비어 있음 — 필요한 항목만 채운다
docker compose up -d
```

`DRY_RUN=true` 가 기본값이다. 실제 주문 경로를 활성화하지 않는 한
거래소로 주문이 나가지 않는다.
